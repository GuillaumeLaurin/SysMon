#include "core/DIContainer.hpp"

/**
 * @file DIContainer.cpp
 * @brief Translation unit for DIContainer. All members are templates
 *        defined inline in the header; this file only ensures the header
 *        compiles standalone.
 */